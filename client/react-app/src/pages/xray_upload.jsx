function XrayUpload() {
  return (
    <section>
      <h1>AI X-ray Upload</h1>
      <p>
        This page will host the full drag-and-drop uploader, advanced settings,
        and run-detection workflow for the AI model.
      </p>
    </section>
  );
}

export default XrayUpload;
